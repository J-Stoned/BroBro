# ============================================================================
# GoHighLevel OAuth 2.0 Credentials
# ============================================================================
# SETUP INSTRUCTIONS:
# 1. Go to https://marketplace.gohighlevel.com/apps
# 2. Create a new app or select existing app
# 3. Navigate to OAuth Settings
# 4. Set redirect URI to: http://localhost:3456/oauth/callback
# 5. Configure scopes (see below)
# 6. Copy Client ID and Client Secret here
#
# REQUIRED SCOPES:
# - contacts.readonly, contacts.write
# - workflows.readonly, workflows.write
# - calendars.readonly, calendars.write
# - opportunities.readonly, opportunities.write
# - locations.readonly
# - forms.readonly, surveys.readonly
#
# SECURITY: Never commit actual credentials to git!
# ============================================================================

GHL_CLIENT_ID=your_client_id_here
GHL_CLIENT_SECRET=your_client_secret_here
GHL_REDIRECT_URI=http://localhost:3456/oauth/callback

# Token Encryption Key
# Generate a secure 32-byte hex key with this command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Never commit this key to git - keep it secret!
ENCRYPTION_KEY=your_64_character_hex_key_here

# Optional: ChromaDB Configuration
# If using a non-default ChromaDB instance
# CHROMA_URL=http://localhost:8000

# Optional: Rate Limiting Configuration
# Override default GHL API limits if needed
# RATE_LIMIT_BURST=100
# RATE_LIMIT_DAILY=200000

# Optional: Logging Level
# Set to DEBUG for verbose logging during development
# Options: DEBUG, INFO, WARN, ERROR
# LOG_LEVEL=INFO
